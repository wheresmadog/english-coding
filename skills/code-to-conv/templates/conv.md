## Current behavior
<!-- Numbered list. Each item is exactly one sentence describing one aspect of what the code does today. -->
1.
2.

## Future directions
<!-- One ### <name>: <one sentence> subsection per candidate key change — same
     naming style `/conv-to-issue` later freezes. Under each, cover the
     problem/motivation and how it works; fold in constraints, rejected
     alternatives, or open questions as prose only where relevant, not as
     separate headings. -->
### <name>: <one sentence>
-
